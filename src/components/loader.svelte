<span
    class="w-12 h-12 border-[5px] border-white border-b-[var(--primary-colour)] rounded-[50%] inline-block box-border"
></span>

<style>
    span {
        animation: rotation 1s linear infinite;
    }

    @keyframes rotation {
        0% {
            transform: rotate(0deg);
        }
        100% {
            transform: rotate(360deg);
        }
    }
</style>
